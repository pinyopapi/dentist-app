export const getAppointments = async (req, res) => {
  res.json({ message: "getAppointments works" });
};

export const createAppointment = async (req, res) => {
  res.json({ message: "createAppointment works" });
};
